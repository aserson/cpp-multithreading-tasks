#include <iostream>

int main() {
    std::cout << "This is empty main funstion for thread tasks project" << std::endl;
    std::cout << "Look at include folder to see tasks code of thread project" << std::endl;
    std::cout << "Look at test folder to see tests code of thread project" << std::endl;

    return 0;
}